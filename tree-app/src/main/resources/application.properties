server.port=8080

# 1. Tu estrategia de algoritmos
app.tree-strategy=collections

# 2. Tu interruptor central (Elige entre: postgres o mongo)
app.storage=memory

# Mapea e incluye automáticamente el archivo properties correspondiente
spring.profiles.include=${app.storage}